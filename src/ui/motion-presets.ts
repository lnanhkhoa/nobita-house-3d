/** Shared `motion` gestures for chrome buttons, so every press feels the same. */

/** Brief press squeeze: the 90 ms press token from docs/design-guidelines.md §4. */
export const press = { scale: 0.95, transition: { duration: 0.09 } };
